# plan.md — <name>

> 브릿지 JSON에서 생성된 코드 계획서. 딸깍 code 단계의 입력.

## 개요
- 출처 브릿지: `.ddalkak/bridge/<name>.bridge.json`
- 대상 화면: <!-- ... -->

## 컴포넌트 분해
- [ ] `<Component>` — 역할 / 대응 브릿지 노드

## 파일 계획
| 경로 | 신규/수정 | 설명 |
|------|-----------|------|
|      |           |      |

## 디자인 토큰 매핑
<!-- 등록 토큰 + 화면에 쓰인 비토큰 raw 값(radius/gap/폭 등)의 distinct 집합을 모두, 최종 코드 표현으로 확정.
     code는 이 표를 복사만 하고 재환산하지 않는다 (plan-rules §4). -->
| 브릿지 값 | 코드 표현 (최종) |
|-----------|------------------|
|           |                  |

## 인터랙션 & 상태 전환
<!-- "이거 누르면 뭐가 되는가"는 정적 디자인에 없다. 브릿지 상태 variant(adaptive)·프로토타입
     reaction에서 도출하고, 불명확하면 추측하지 말고 아래 ⚠에 질문으로 올린다 (plan-rules §11).
     오버레이 역할(bottom-sheet/dialog/toast)은 페이지가 아니라 모달로 계획한다(code-rules §8-1). -->
| 트리거 (무엇을) | 동작 (어떻게) | 로직 출처 / 미상이면 ⚠ |
|-----------------|---------------|------------------------|
|                 |               |                        |

## 기존 소스 통합 (브라운필드)
<!-- 이 화면이 건드리는 기존 화면·라우트·API·l10n·**테스트**. 대체/수정/공존/신규 판정 + 대체·수정 시
     보존할 것(동작·위젯 Key·테스트 = 회귀 경계). 그린필드면 "해당 없음". plan-rules §12. -->
| 기존 자산 | 관계 (대체/수정/공존/신규) | 보존할 것 (회귀 경계) |
|-----------|----------------------------|------------------------|
|           |                            |                        |

## 구현 순서
1. <!-- ... -->

## ⚠ 결정·추가정보 요청 (사용자 게이트)
<!-- 규칙으로 결정 못 해 사용자가 정해야 하는 것 + 디자인에 없어 추가정보를 받아야 하는 것.
     추측으로 채우지 않는다. 하나라도 있으면 code로 넘어가기 전에 사용자에게 **구체적 선택지로** 묻고
     답을 받아 여기에 "확정"으로 바꾼다 (plan-rules §13). 없으면 이 섹션 삭제. -->

